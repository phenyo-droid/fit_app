import 'package:flutter/material.dart';

class HomePage extends StatlessWidget{
  const HomePage({super.key});

  @override
  Widget build(BuildContext context){
    return Scaffold(
      appbar: AppBar(),
    )
  }
}

